# data branch

Snapshots of the feed data published on https://boenwip.github.io/cyber-dashboard/.

Written only by `.github/workflows/deploy.yml` on `main`: each run starts from the
latest snapshot here, fetches fresh data, publishes the site, and commits the new
snapshot back to this branch. Nothing here is edited by hand.
